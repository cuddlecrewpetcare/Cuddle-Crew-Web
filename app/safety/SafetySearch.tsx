'use client';
import {useMemo,useState} from 'react';
import {trackPublicEvent} from '../lib/public-analytics';
export type SafetyEntry={category:string;title:string;body:string};
export default function SafetySearch({entries}:{entries:SafetyEntry[]}){const[query,setQuery]=useState(''),[category,setCategory]=useState('All');const categories=['All',...new Set(entries.map(x=>x.category))];const results=useMemo(()=>{const q=query.trim().toLowerCase();return entries.filter(x=>(category==='All'||x.category===category)&&(!q||`${x.title} ${x.body} ${x.category}`.toLowerCase().includes(q)));},[query,category,entries]);return <section className="content-card safety-search"><h2>Search the what-if center</h2><div className="safety-controls"><label>Search safety topics<input type="search" value={query} onChange={e=>{setQuery(e.target.value);if(e.target.value.trim())trackPublicEvent('safety_searched')}} placeholder="Example: lost key, medication, wildfire"/></label><label>Category<select value={category} onChange={e=>setCategory(e.target.value)}>{categories.map(x=><option key={x}>{x}</option>)}</select></label></div><p role="status">{results.length} {results.length===1?'topic':'topics'} found</p>{results.length?<div className="accordion-list">{results.map(entry=><details key={`${entry.category}-${entry.title}`}><summary><span>{entry.title}</span><small>{entry.category}</small></summary><p>{entry.body}</p></details>)}</div>:<div className="empty-state"><h3>No matching safety topic</h3><p>Try a broader word or choose All categories. You can also ask Lauren about a situation that is not listed.</p></div>}</section>}
