import type {MetadataRoute} from 'next';
export default function sitemap():MetadataRoute.Sitemap{if(process.env.SITE_INDEXING_ENABLED!=='true')return[];const base='https://www.cuddlecrewpetcare.com';return['','/start','/plan','/holidays','/choosing-care','/safety','/credentials','/faq','/contact','/privacy','/terms'].map((path)=>({url:`${base}${path}`,changeFrequency:path?'monthly':'weekly',priority:path?0.7:1}));}
