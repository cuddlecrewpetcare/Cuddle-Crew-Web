# Overnight Dog Walk add-on compatibility

## Assignment and recovery

- WORK_ITEM: `12G-EST-02`; STATUS: IMPLEMENTATION AUTHORIZED. Exact final base hosted Validation [34492100008](https://github.com/cuddlecrewpetcare/Cuddle-Crew-Web/actions/runs/34492100008) SUCCESS.
- OWNER: fresh native `/root/est02_implement` completed preflight and the bounded initial implementation, then stopped when the account-wide native-agent usage limit interrupted its turn. Parent `/root` assumed the documented isolated parent-writer fallback on 2026-09-12 after confirming the agent was no longer running, the exact worktree/base, preserved diff, healthy dependencies and free test port. Parent remains integration owner and will not self-approve; fresh independent review remains required.
- PREPARATION_BASE: `7a1f8affbbcf8b60b34a7c1e2a217789edc03c86`; not the final implementation base. Branch `codex/12g-estimator-compatibility-20260910`; isolated `estimator-compatibility` worktree in managed workspace.
- BASE_SHA: `1c4956dde47fc5f35b191bbc76e9b7a2f653f56a`, fast-forwarded to current main after EST-01 integration; re-inspect its narrow diff before edits. Baseline 196 Node / 59 Playwright.
- AUTHORIZATION: standing release sprint, bounded existing P1 correction. No routine additional owner approval. Final integrated audit/publication remain later gates.
- OBJECTIVE: an incompatible cat-only or mixed-care household and Overnight Dog Walk add-on cannot receive an ordinary automatic estimate.
- MODEL: Astra / high recommended for bounded business/state implementation. Runtime identity is assignment-scoped; speed unknown.
- READ_SCOPE: mandatory contracts, complete original audit, business README/full hierarchy, current approved pricing/scope references and related implementation/tests.
- WRITE_SCOPE after CI_GO: minimal estimator validation/component paths, type/state changes only if directly necessary, focused regression tests and this RUN. Parent owns status and prior EST-01 completion receipt.
- FORBIDDEN: fabrication of household species, new service-component model, business-policy changes, unrelated P1/P2/P3, dependency/lockfile/provider/Sites/DNS/indexing/SMS/payment/PPC changes, original or other checkout writes, child agents, worker push/merge.

## Reconciliation and acceptance

Read CURRENT / APPROVED core/03 pricing sections 3/4 and Website Notes, logic/38 sections 5 and 7, applicable safety/scope references and full hierarchy. A dedicated Dog Walk cannot pretend to provide substantive non-dog care without an approved separate care component. A representable selection is not automatic accepted service.

Original audit: cat-only Overnight plus 30-minute Dog Walk add-on returned ordinary $112. BUS-02 already corrected primary Dog Walk service transitions that rewrote known pets as dogs; preserve that merged correction and do not repeat it. The remaining finding is the Overnight add-on compatibility path.

- Direct domain/API/UI: incompatible cat-only or mixed-care Overnight walk cannot produce ordinary automatic pricing; show a conservative, clear recovery/review state.
- Check the effective active add-on, including an explicit Planner daytime selection that supersedes a stale raw add-on. Do not reject a valid Planner Drop-In based on an ignored old Dog Walk field.
- Preserve known composition, opaque review and restoration confirmation. Do not silently switch species or service to create a price.
- Preserve supported all-dog Overnight plus 30-minute walk $117 and 60-minute walk $135 when all other conditions are ordinary.
- Test all relevant walk choices, service/add-on changes, saved schema-2 restoration, handoff where relevant and absence of stale numeric copy/print.
- No new mixed-component pricing design; existing neutral review/validation architecture is sufficient.

## Gate and handback

Doctor/setup already performed in prepared worktree (505 locked packages; no dependency changes). No browser server until parent grants port3100. Before edits reconcile new exact base and prior source diff. Focused direct pre-fix reproduction and tests; full prescribed foundation/secrets/typecheck/lint/build/full gate, actual counts/warnings/failures/skips, then exact-path commit. Parent obtains fresh independent review, source CI, normal no-ff merge, merged local gate and exact-main CI. No overall GO claim.

## Implementation handoff and fallback

Pre-change reproduction on exact base: cat-only Overnight with 30/60/90-minute Dog Walk add-ons returned ordinary totals $112/$130/$148; dog-plus-cat returned $122/$140/$158. Supported dog-only anchors were $117/$135/$153. A Planner-owned explicit no-add-on or 30-minute Drop-In correctly superseded an older raw Dog Walk selection at $80/$103 for cat-only care.

The native Implementer introduced a public-selection-only helper used by both domain validation and the estimator UI. It resolves the effective add-on after Planner precedence, then applies the existing dog-walk household compatibility rule. The UI preserves entered species and shows a neutral personalized-review recovery message while withholding stale estimate, copy and print output. Focused domain/API and existing boundary tests passed 53/53 before the native turn ended. The parent fallback independently inspects, validates and commits this exact bounded work; independent review remains external to both writers.

## Parent fallback validation

The fallback retained the native Implementer's bounded design and corrected only two TypeScript assertions in the new API regression test after the first typecheck exposed `Response.json()` as `unknown` and a possibly-null result. No application behavior changed in that correction. Focused cross-feature Node validation passed 92/92 and focused browser validation passed 4/4. The complete prescribed gate then passed: doctor, foundation, resources, current secret scan (zero findings), 201/201 Node tests, typecheck, lint, build, build-artifact privacy, 63/63 Playwright tests, and `git diff --check`, with zero failures or skips. Expected warnings were the documented optional/WASM supply-chain entries, image budget notices, Vinext route-classification notice, Node type-stripping/color notices, synthetic provider failures, and safe availability rate-limit fallbacks. Build artifact safety reported 178 files / 10.36 MiB and made no source edits. No real provider call or external write occurred. Exact-source independent review and hosted Validation remain required before integration.
