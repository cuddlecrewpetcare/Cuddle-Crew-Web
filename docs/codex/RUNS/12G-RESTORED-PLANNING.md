# Restored planning-state safety

## Assignment and recovery

- WORK_ITEM: `12G-CARE-03`; verify the related direct-estimator restoration candidate `12G-STATE-01` before classifying it.
- STATUS: IMPLEMENTED / READY FOR INDEPENDENT EXACT-SHA REVIEW; exact merged-main Validation `34479764616` SUCCESS at the assigned base. Product work authorized after required preflight.
- OWNER / EXECUTION: fresh native `/root/restore_implement`; parent/integration owner `/root`, release-completion session of 2026-09-10.
- BASE_SHA: `303be467db21040881449c6a799b1d61935b7c25`.
- BRANCH: `codex/12g-restored-planning-safety-20260910`.
- WORKTREE: isolated `restored-planning` in this session's managed workspace. Protected original `C:/Dev/CuddleCrewPetCareWEB` is never a writer.
- AUTHORIZATION: owner's standing release sprint covers recorded and newly proven bounded P0/P1 corrections, validation, fresh review and normal integration. Publication still requires a fresh integrated 12G GO.
- WHY / USER_INTENT: preserve pet safety while finishing the release; privacy minimization must not turn omitted safety answers into benign answers after refresh.
- OBJECTIVE: restored incomplete, malformed, stale or review-required planning state cannot produce ordinary eligibility or automatic pricing through missing information. Keep existing session stores and clear/reset controls, retain safe broad selections where possible, and never persist private answers or reasons.
- RESOURCE_ASSESSMENT: safety and cross-feature restoration merit strong bounded reasoning; Astra / high recommended for implementation. No desired/actual speed assumption.
- READ_SCOPE: relevant application/state/API consumers, required repository contracts and most-specific approved business authority.
- WRITE_SCOPE: existing Planner persistence/helper/component and tests; existing estimator persistence/helper/component and directly necessary tests only if the candidate is reproduced. This RUN. Parent alone owns PROJECT_STATUS and prior completion receipts.
- OPTIONAL_SAME_ROOT: if negligible within changed restoration validation, preserve incomplete state for malformed imported coverage. Prior handoff reviewer confirmed invalid string/null windows can be filtered to empty/subsets, but found no ordinary-user P1 path. Do not inflate severity or expand to a general API redesign; otherwise retain as deferred defense in depth.
- FORBIDDEN: unrelated P1/P2/P3 work, pricing/policy changes, dependencies/lockfile, provider/hosting/DNS/indexing/SMS changes, private persistence, original checkout or other worktree edits, child agents, worker push/merge/deployment.

## Reconciliation and direct before evidence

CARE-03 is UNRESOLVED. Original audit: a complete Planner scenario with broad safe inputs and aggressive behavior requires consultation; refresh preserves broad inputs but resets the omitted behavior answer to `none` and recomputes an ordinary starting point. Parent independently reproduced the same domain transition at prior product-identical main `43553cb`: consultation-required becomes starting-point; private behavior is correctly absent from saved state. The handoff merge does not change the Planner persistence helper.

Related candidate STATE-01: directly selecting the estimator's detailed-care checkbox requires review, but session persistence stores pet types without the private checkbox or an opaque review requirement. Reload initializes `complex:false`. Parent's source-matched domain reproduction changed review true to false and returned ordinary $30 with a synthetic Standard travel tier. Verify through a rendered fresh-session/refresh reproduction against this current base before declaring it an additional P1. The shared root is omitted private information being interpreted as benign on restoration; if confirmed, correct that same restoration invariant through the second existing store with separate direct evidence.

## Business Truth and acceptance

Read business README and source hierarchy, then CURRENT / APPROVED logic/18 Automated Acceptance vs. Review, logic/22 medication scope, logic/23 behavior review, logic/33 review boundaries and privacy; current docs/data-privacy.md and testing-quality.md. Read the complete original Phase 12G audit before implementing. Read core/03 and logic/38 only where estimator output meaning requires them. No authority conflict was found by the prior read-only preflight; verify current references independently.

Required matrix: fresh state, complete valid saved state, partial payload, invalid JSON/nonobject/invalid fields, stale or unknown schema, and review-required restored state. Missing or unknown safety answers must not mean No. Fresh-state behavior and valid explicit re-entry remain usable. Review carried by broad fields (including four dogs/CARE-01, young pets, invalid fit or unsafe gaps) cannot be cleared by restoration or reconfirmation. Sensitive omitted answers and task/routine details remain absent from session storage, URLs, analytics and logs. Only minimal nonsensitive restoration/review provenance may be retained. Use conservative incomplete/reconfirmation behavior until the necessary answers are explicitly supplied again.

Preserve the merged handoff's exact coverage/composition and review contract, copy/print behavior, location-only handoff and explicit empty selections. Do not introduce another state store or resurrect old hidden services. Each finding actually fixed needs its own direct Node/API or browser evidence as applicable.

## Required gates and completion

Targeted regressions during iteration; doctor, Git safety, foundation/resources/secrets, full validation, diff/staged-scope checks at completion. Report actual Node/E2E/accessibility counts, all warnings/skips and diagnosed iteration failures. Commit only exact owned paths and the parent-owned completion/status documents unchanged; no push or merge. Fresh independent exact-SHA review and exact-head/merged-main hosted Validation are parent gates.

Implementation evidence and exact changed paths are recorded below; use the containing commit from Git history for its SHA. Independent review, hosted source CI, merged SHA and final disposition remain PENDING parent gates.


## Implementer checkpoint — 2026-09-10

Native owner `/root/restore_implement` verified exact base `303be467db21040881449c6a799b1d61935b7c25`, assigned branch and worktree, expected GitHub/Sites remotes, and intentional parent-owned status/HANDOFF edits. Doctor was READY LOCALLY; Git safety passed. No dependency install/change was needed by this worker. Original dirty checkout and other worktrees were not written.

### Direct before evidence and authority

Both browser reproductions ran against the unmodified assigned base, with a local production build, synthetic inputs and no enabled provider writes:

- **12G-CARE-03, P1:** one adult dog, 24-hour entered intervals, 30-minute fit, selected daytime windows, and an aggression-related disclosure rendered `Personalized review required`. Reload restored the broad values and rendered `30-minute visits may be a useful starting point`; the omitted behavior answer had become the benign default.
- **12G-STATE-01, newly confirmed P1:** the direct estimator's detailed-care checkbox was checked. The saved record contained pet type, service, windows, midpoint, ZIP and availability, without the checkbox or neutral review provenance. After adding a synthetic Standard travel tier to isolate travel from care review, reload made the checkbox unchecked. Re-entering synthetic future dates produced a real estimate API response with `reviewRequired:false` and `total:30`. Parent confirmed the separate P1 classification within the existing same-root assignment.

Controlling CURRENT / APPROVED references read: business README and complete source hierarchy; logic/18 Booking Acceptance / Risk Triage (Automated Acceptance vs. Review, required information, review/privacy and safety override); logic/22 Medication Scope Review (automatic review, missing instructions and privacy); logic/23 Behavior Risk Review (incomplete disclosures, review triggers, no inferred behavior information and privacy); logic/33 Custom Quote / Scope Review (review boundaries, material uncertainty, public/private result separation); core/03 Pricing, Fees & Surcharge Policy and logic/38 PPC Pricing & Quote Implementation for estimate/subtotal and handoff meaning. The complete original Phase 12G audit was reviewed. No approved-source conflict or business-rule change was identified. DRAFT/PLACEHOLDER material was not authority.

### Implementation and privacy boundary

- Existing session keys remain unchanged. Each store now writes a version-2 envelope. Recognized complete broad selections may restore; missing, old/unknown-schema or malformed inputs cannot silently acquire valid eligibility. Planner invalid/stale state requires a new visit-fit answer; estimator invalid/stale care state stays incomplete until reset or a new complete Planner handoff.
- Every restored Planner requires explicit selection of its four omitted care answers plus confirmation of household/interval/window/fit/task selections. Omitted task selections start empty. Until completion the domain assessment requires review, selects no duration, and the UI offers no pricing handoff. Confirming does not bypass four-dog, young-pet, unsuitable-duration or unsafe-gap review.
- Every restored estimator care record requires explicit confirmation before an estimate request/result, summary or price is available. A neutral review requirement is retained through the existing store and existing Planner API review contract; it remains reviewed after reconfirmation and can be removed only by the existing clear/reset action. Dates and omitted care checkbox values remain unsaved. Valid ordinary state can return to ordinary estimates only after explicit confirmation.
- A consumed care-handoff query is removed from the current history entry after hydration, preserving other query fields and fragment, so refresh cannot replay an earlier benign handoff over a later care disclosure. New complete Planner handoffs continue to replace stale care selections and preserve only location. Location-only restoration remains usable without a care-confirmation gate.
- Malformed stored Overnight window arrays remain incomplete instead of accepting filtered empty/subset coverage. This is bounded defense in depth in the changed storage parser, not a new P1 or general API redesign; broader query/API validation remains deferred.
- New data consists only of schema metadata, transient confirmation provenance, and neutral review status without reasons: INTERNAL/low-risk, SESSION-LIMITED in the same user-clearable browser-tab store. No extra store, database, provider, retention class or Client record is introduced. Medication/behavior/separation answers, care detail checkbox values, tasks, private reasons, identity, exact addresses and dates remain outside persistence/URLs/logging.

### Validation checkpoint and diagnosed iterations

- Initial targeted Node run: 47/48 passed; the one existing assertion expected the old default object without confirmation metadata. Updated it to require the new conservative restoration flag; no behavioral assertion was removed. Targeted Node run then passed **54/54**, including six new restoration cases and the existing care/state/handoff suites.
- Initial typecheck identified one new TypeScript narrowing issue in the window validator; corrected its type predicate. Typecheck then passed.
- Initial affected browser run: **20/21 passed**. The new care test used an exact label selector that also encompassed option/description text and did not resolve. Corrected the locator to the existing unique visible-label pattern; assertions and the 30-second timeout were unchanged. Focused restoration rerun passed **6/6**, including both restored-state Axe scans.
- Initial foundation scan stopped on two false positive generic-key matches caused by repeating public session-key constants in the new E2E fixture. Reused the existing exported constants instead; no credential existed, and no scanner rule or exception was changed.
- Foundation passed after the fixture correction. The first full gate stopped at a newly appended RUN punctuation byte written with the Windows default encoding. Re-encoded only this owned RUN as UTF-8, and verified the cross-platform guard before rerunning full validation. No source behavior or validation rule changed.
- Final `npm run validate:full`: PASS. **186/186 Node** and **47/47 Playwright** passed, zero failures/skips/retries. This is +6 Node and +6 Playwright against the assigned 180/41 baseline; no test was removed. The full browser suite includes all **15/15 existing accessibility cases** plus the new combined case with **two restored-state Axe scans**, both zero violations.
- Typecheck and lint PASS, lint zero errors/warnings; production build PASS; artifact shape/privacy PASS (**178 files / 10.36 MiB**). `check:foundation` PASS; current secret scan and history secret scan both **0 findings**. Final cross-platform/diff/Git scope checks PASS.
- Existing warnings retained: intentional OG image 1.75 MiB and Loki image 2.74 MiB (Git/resources); five npm extraneous optional/WASM entries; Node experimental type stripping; Vinext static route classification limitation; Playwright NO_COLOR/FORCE_COLOR notice. No new dependency/audit work, suppressions, test skips or unresolved validation failures.
- Exact commit is the containing implementation commit, discoverable through Git history for this RUN; final SHA is supplied in the native completion receipt. Hosted source Validation and independent review are PENDING parent gates.

Six new Node tests cover privacy-preserving save/load, each omitted safety answer, fresh/confirmed behavior, malformed/partial/old/unknown schema, enum/count/array boundaries, preserved broad review, neutral direct-estimator review, location and clear/reset, and invalid stored Overnight coverage. Six new browser tests cover all four Planner disclosures/reconfirmation/tasks, stale/malformed progress, direct-estimator review with real API/copy/print, ordinary state after confirmation, handoff-replay prevention, and restored-state accessibility. Existing merged handoff and public-flow tests retain their assertions and now acknowledge the required confirmation step/unanswered restoration state.

Business requirements changed: NO. Independent exact-SHA review, source CI and normal integration remain parent gates. No push, merge, deployment, Sites operation or provider write was performed. Revert the final bounded commit through normal reviewed integration if rollback is needed; persisted version-2 envelopes remain privacy-minimized and resettable, but the previous implementation must not be represented as restoration-safe.


### Exact completion scope

Product: `app/QuoteEstimator.tsx`, `app/plan/CarePlanner.tsx`, `app/lib/care-planner.ts`, `app/lib/care-planner-progress.ts`, `app/lib/planning-state.ts`.

Tests: `tests/restored-planning.test.ts`, `tests/feature-completion.test.ts`, `e2e/restored-planning.spec.ts`, `e2e/planner-handoff.spec.ts`, `e2e/public-flows.spec.ts`.

Evidence: this RUN. The parent-owned `docs/codex/PROJECT_STATUS.md` and `docs/codex/RUNS/12G-HANDOFF.md` are included unchanged from their intentional parent edits. No other paths belong in this commit.

Final environment, Git/status and exact-SHA clean-commit verification are reported in the native completion receipt. Port 3100 is released by the owned E2E lifecycle after the full pass. Parent may begin source CI and independent review; application GO/publication remains gated on the rest of the release work and fresh integrated 12G evidence.

## Parent integration receipt

Exact implementation `d8a33f0fcb6e093f424002ba82f069ee68687fb4` was independently APPROVED by fresh read-only `/root/restoration_review`, no REQUIRED or OPTIONAL findings. Independent 54 Node / 22 Playwright tests plus 62 additional state-boundary assertions passed, including real API, copy/print and two restored-state Axe scans; secrets 0, clean exact head. Hosted exact-source Validation [34482982668](https://github.com/cuddlecrewpetcare/Cuddle-Crew-Web/actions/runs/34482982668) SUCCESS with actual 186 Node / 47 Playwright and zero failures/skips.

Normal no-ff integration `113391c4964e33578dd171d8387fff32dd0d22cd` has parents `303be467db21040881449c6a799b1d61935b7c25` and the implementation SHA. Tree `375ed3b80a2070434b461fc036f249442b3ade0f` matches reviewed source and merge simulation. Merged local doctor/full gate passed 186 Node / 47 Playwright (2.4 minutes E2E), typecheck/lint/build/artifact/current secrets, clean diff/status. Merged artifact measured 189 files / 10.51 MiB; its 11 emitted Geist font files (146,464 bytes) account for the count/size difference from the source checkout's font-fallback build. Privacy/resource checks and rendered tests passed both. No generated output was edited. Release will use its required clean exact-SHA checkout and artifact.

Pushed normally to GitHub main; PR #12 is MERGED. Exact merged-main hosted Validation [34484045393](https://github.com/cuddlecrewpetcare/Cuddle-Crew-Web/actions/runs/34484045393) SUCCESS for `113391c4964e33578dd171d8387fff32dd0d22cd`. CARE-03 and newly confirmed STATE-01 are CLOSED with their separate direct evidence above. No production operation or provider setting changed.
