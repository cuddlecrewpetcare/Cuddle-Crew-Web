# One-time orchestration bootstrap — 2026-09-09 UTC

> WORK_ITEM: CC-BOOTSTRAP
> Scope: repository/Codex/GitHub audit and compact governance documentation.
> Owner: Orchestrator task `01a084b5-4bc7-7273-851b-fb747949d2b1`.
> BASE_SHA: `60572ae5c7c24caa3fd76d49f997c763d8eb0718`.
> BRANCH: `codex/orchestration-bootstrap`.
> FINAL_SHA: containing focused bootstrap commit; resolve with `git log -1 --format=%H -- docs/codex/RUNS/2026-09-09-bootstrap.md`.
> STATUS: IN_REVIEW at commit-time handoff; local validation and independent diff review passed. Final committed-SHA review/CI belongs in the associated PR/check record, avoiding a self-updating evidence commit loop.
> RISK / IMPACT: MEDIUM / INTERNAL_ONLY, OPERATIONS. Cross-cutting governance; no executable configuration change.
> MODEL / REASONING: strong model / Ultra explicitly requested for this one-time bootstrap. Actual executing model not independently queried. No global setting changed.
> ROLLBACK: revert the focused documentation commit in a separately scoped normal change; no production rollback required.

## Repository evidence and preserved state

- Canonical checkout: local `main` at `3b443a6dcafb0cb3f4ed4129714d357e3e059816`, tracking `github/main`, behind by 53 commits. It is intentionally dirty.
- Remote default/base: `main`; `git ls-remote github refs/heads/main` directly returned `60572ae5c7c24caa3fd76d49f997c763d8eb0718`.
- Pre-existing tracked dirt: ten modified paths (eight app files and the two package manifests) and seventeen deleted legacy public images. Untracked: local Codex environment config, accessibility CSS/test, recovered planning, owner media/resource directories and one anomalously named historical file. Full porcelain inventory: 61 entries. This was not bootstrap work.
- Ignored dependency/build/emulator/test caches and `tsconfig.tsbuildinfo` were inventoried as generated/local state. No secrets file was opened.
- A separate worktree starts from the verified remote SHA. No canonical reset, clean, restore, stash, stage, checkout, content edit, asset deletion, worktree pruning or history rewrite is part of bootstrap. Git worktree metadata/one new branch are intentional.
- Root README/AGENTS/CONTRIBUTING, tracked docs/planning/phases, scripts, package/lockfile, test/lint/typecheck/build config, deployment manifest/environment template, workflows and public content entrypoints were inspected. No tracked nested AGENTS, existing orchestration directory, dedicated status ledger or ADR directory was found.
- Local untracked `.codex/environments/environment.toml` is autogenerated and provides setup/dev/doctor/Git/foundation/full-validation actions. It was retained without changes.
- Runtime: Node 22.17.1, npm 10.9.2, npm lockfile v3, Vinext 1.0.0-beta.3 with Next 16.3.4/React 19.2.6. No version, package, lockfile, script, application, asset, business-reference or hosting-manifest change is in scope.

Recent lineage: `60572ae` merges the `2dde9c9` 12G audit; application `d828c97` merges 12F (`caaa78c`, `4a8580c`); `62aa602` merges 12E. Completed phase history remains intact.

## Current project authority and state

- LATEST_VERIFIED_PHASE: 12G audit completed and merged, **APPLICATION NO-GO**; last implemented application phase 12F.
- ACTIVE_PHASE: orchestration bootstrap only. NEXT_AUTHORIZED_PHASE: none beyond this bootstrap.
- CURRENT_BLOCKERS: twelve P1 application findings in [12G §R](../../phase-12g-final-release-audit.md); required correction sequence and the 12H gate are in §W. Seven P2/five P3 remain existing backlog; two P2 owner-evidence items also gate launch.
- AUTHORITATIVE_BUSINESS_SOURCE: [business README](../../business-reference/README.md) and [complete hierarchy](../../business-reference/guidance/source-of-truth-document-hierarchy.md), with the most specific applicable CURRENT / APPROVED source. No hierarchy or business values were duplicated in operational docs.
- AUTHORITATIVE_PROJECT_PLAN: current explicit Lauren scope plus the applicable reconciled phase specification. There is no blanket execution grant in a living roadmap. [Recovered roadmap](../../phase-12-roadmap-recovery.md) and [historical index](../../planning/historical-prompts/CURRENT-RECONCILIATION.md) expressly require reconciliation and are non-authoritative; their old future-12E/12F descriptions are stale.
- AUTHORITATIVE_ARCHITECTURE: [CONTRIBUTING](../../../CONTRIBUTING.md), [testing](../../testing-quality.md), [privacy](../../data-privacy.md), [integrations](../../integrations-side-effects.md), [deployment](../../deployment-hosting.md), and the other existing topic contracts referenced by ORCHESTRATION.
- EXISTING_STATUS/HANDOFF: exact-SHA phase reconciliation/audit/completion docs and Git/native task evidence. The new short ledger is only a cache.
- EXISTING_BACKLOG: 12G §R/§T/§S; no second issue tracker or roadmap created.
- EXISTING_ORCHESTRATION: local Codex environment actions and manual bounded phase/merge tasks; no durable role/ownership protocol existed.
- EXISTING_DEPLOYMENT_MODEL: separate GitHub mirror/validation and explicit Sites source/version/activation. A GitHub push/merge does not deploy.

Authority status audit found core 01–03, logic references, guidance, and operations 35/38 CURRENT / APPROVED; core 04–10/15 are drafts and operations 40 is PLACEHOLDER. These are snapshot observations, not permanent status declarations. No material conflict between approved sources was established; 12G documents implementation-versus-authority defects. Older resolved blockers (for example the pricing manual's historical dual header) must not return through stale reports.

Business references used: business README, hierarchy, and [logic/33](../../business-reference/logic/33-custom-quote-scope-review.md) §§2–3 for pilot selection. No pricing/policy content changed. Future work must read its own specific current references.

## Codex capability snapshot

CONFIRMED below means exposed in this runtime; exercised operations are identified. It does not imply every future host supports the same tools or that destructive operations were tested.

| Capability | Result | Evidence / limit |
| --- | --- | --- |
| CROSS_TASK_DISCOVERY | CONFIRMED | `mcp__codex_app.list_projects/list_threads` exercised |
| CROSS_TASK_CREATE | CONFIRMED | `create_thread` exposed, including Git worktree targets; not used to create idle roles |
| CROSS_TASK_SEND / RECEIVE | CONFIRMED | `send_message_to_thread`, `read_thread`, `wait_threads` exposed; reading existing project task exercised; app-to-app delivery not live-tested |
| TASK_NAMES / TASK_IDS | CONFIRMED | Tool responses contain titles, IDs and host IDs; titles are not routing keys |
| TASK_DEEPLINKS | UNKNOWN | No stable task deeplink returned in inspected responses; do not fabricate one |
| TASK_STATUS | CONFIRMED | Discovery/read tools expose status; status can lag completed artifacts |
| MESSAGE_DELIVERY_ACKNOWLEDGEMENT | UNKNOWN | No end-to-end app read/execution acknowledgement demonstrated; protocol requires recipient ACK |
| SUBAGENTS | CONFIRMED | Independent read-only audits exercised with `collaboration.spawn_agent`; send/receive and completion observed |
| WORKTREES | CONFIRMED | Existing worktrees inspected; isolated bootstrap worktree created at verified base; native create/fork/handoff also exposed |
| STEER | CONFIRMED | Subagent followup/send/interrupt and app follow-up send concepts exposed |
| QUEUE | UNKNOWN | Pending setup/wait concepts exposed; no durable exactly-once FIFO queue guarantee demonstrated |
| GITHUB_REPOSITORY_ACCESS | CONFIRMED | Remote identity/main read; GitHub connector and gh exposed |
| GITHUB_PR_ACCESS / CI_STATUS_ACCESS | CONFIRMED | Connector fetch/list/diff/check/log tools exposed; remote audit below records actual access |
| PR_CREATION / PR_REVIEW / MERGE_ACTIONS | CONFIRMED | Native GitHub create_pull_request/add_review_to_pr/merge_pull_request and gh exposed; availability is not standing merge authority |

Native capabilities are sufficient for the operating design. A fresh assignment records ready task ID, owner, work item and base; uncertain delivery uses status inspection then an idempotent retry or the same RUN assignment as a manual fallback. No messaging framework, background schedule or extra permanent task was installed.

Model choices exposed by app task tools: gpt-6-astra, gpt-5.6-sol, gpt-5.6-terra, gpt-5.6-luna, gpt-5.5, gpt-5.3-codex-spark. Subagents expose the same set except Spark. Availability and supported effort are tool-schema facts, not price quotes. Least-capable-cost policy maps Luna/low to deterministic work, Terra/medium to ordinary coordination/coding/review, and Sol/Astra only where difficulty warrants. Exact account billing and actual per-worker runtime model are UNKNOWN unless returned.

The [official worktree documentation](https://learn.chatgpt.com/docs/environments/git-worktrees) supports independent branch work and notes shared Git metadata. The actual session tool schema governs app-specific operations. No production environment values are copied into fresh worktrees.

## GitHub / CI audit

Repository workflow inspection:

| Item | Finding |
| --- | --- |
| TRUSTED_BRANCH_CI / CODEX_BRANCH_CI | Same-repository PR creation/update triggers Validation; `main` push also triggers it. Bare feature-branch push does not. No manual approve step is in workflow source. |
| FORK_CI | `pull_request` can run fork code in an unprivileged runner after applicable platform approval; keep unknown external code gated and review before execution. |
| WORKFLOW_SECURITY | Single Validation workflow/job; ubuntu-24.04, 35-minute limit, exact source checkout verification, full validation; email writes/indexing off. |
| PULL_REQUEST_TARGET | Absent. No workflow_run/reusable deployment workflow or bot event loop. |
| GITHUB_TOKEN_PERMISSIONS | Workflow explicitly `contents: read`; checkout persist-credentials false. No production secret reference. |
| THIRD_PARTY_ACTIONS | Official actions/checkout, setup-node, upload-artifact pinned to immutable SHAs. Official Gitleaks v8.30.0 download checked against published checksum. |
| ARTIFACTS / CACHE | Failure-only synthetic Playwright artifacts, seven-day retention; npm cache keyed by lockfile. No release artifact promotion, build cache or workflow-created commits/PRs. |
| CONCURRENCY / DISPATCH | Superseded validation cancels by workflow/ref; manual dispatch adds history secret scan. |
| DEPLOYMENT_COUPLING | No CI deployment/Sites/DNS/provider write job or production environment declaration. |
| ACTIONS SETTINGS | API: enabled; allowed_actions all; global SHA pinning not required. The existing workflow independently pins its Actions. Default workflow token read; bot PR approval disabled. |
| FORK APPROVAL SETTINGS | API: first_time_contributors. Repeat outside contributors are not universally approval-gated; never describe this as all-external approval. Recommended tightening below. |
| BRANCH_PROTECTION / RULESETS | API explicitly reports main not protected (404), rulesets empty. No remote enforced required checks. |
| REQUIRED_CHECKS | Project contract requires Validation / validate for relevant PR/merge work; not enforced by remote branch settings. |
| AUTO_MERGE / ENVIRONMENTS | API: auto-merge disabled; zero GitHub Environments. |
| SECURITY_FEATURES | API: repository secret scanning, push protection, non-provider patterns, validity checks, Dependabot alerts and security updates disabled. Code scanning default setup not-configured; no CodeQL or dependency-review workflow. User-level push protection/partner scanning were not queried and must not be inferred disabled. |
| ACCESS | Public repository, authenticated admin/read/push permission confirmed. Initial sandbox network failures resolved with approved read-only escalation; no credential refresh or secret read. |
| CHANGES_MADE | None to GitHub settings or workflows. |
| CHANGES_RECOMMENDED | Keep current CI. Prioritize the small native protections below; no billable/disruptive feature or automatic dependency-update project was enabled. |

Direct gh inspection verified [Validation run 34305243762](https://github.com/cuddlecrewpetcare/Cuddle-Crew-Web/actions/runs/34305243762): successful main push, exact SHA 60572ae5c7c24caa3fd76d49f997c763d8eb0718, successful validate job including source verification and full gate. Baseline CI cannot approve this bootstrap's later commit. Final artifact checks must be read from its PR/source SHA.

**Can Lauren's own / authorized Codex same-repository routine CI run automatically without unnecessary manual approval? YES**, on PR creation/update and main push. Existing triggers already provide this; no auto-approval bot or privilege expansion is needed. Feature push alone intentionally has no run. External code has NOT been granted equivalent privileged execution; routine CI itself has no production privilege. Current first-time-only fork policy is weaker than universal external approval, so human/security review remains required by the operating policy for unknown external code.

Native protection recommendations (not bootstrap blockers or newly authorized projects):

| Feature / observed state | Benefit | Cost / maintenance | Recommendation |
| --- | --- | --- | --- |
| Secret scanning + repository push protection: off | Detect exposed credentials and block supported new leaks | Public-repository feature without paid add-on; low burden, investigate alerts/bypasses | Strongly recommend enabling; keep Gitleaks and never auto-bypass |
| Dependabot alerts: off | Surface known vulnerable dependencies | Available across plans; periodic triage, no automatic product edit | Strongly recommend enabling alerts |
| Dependabot security/version updates: off/no config | Propose dependency repairs/updates | Native feature; PR/CI and compatibility review burden | Optional deliberate maintenance; no auto-merge or major upgrades from this bootstrap |
| CodeQL default setup: absent | Additional static security findings | Public-repository availability; added analysis runs and triage | Consider separately after assessing signal/cost; no duplicate mandatory job now |
| Dependency review gate: absent | Review newly introduced vulnerable dependencies | Public-repository availability; another workflow/check to maintain | Defer dedicated workflow; retain manifest/lockfile review and existing supply-chain gate |
| Non-provider patterns/validity: off | Broader detection/validation | Entitlement and external validation implications not verified | Optional after core protection; no blind enablement |
| Main protection/rulesets: absent | Prevent force push/deletion and enforce meaningful checks | Native control; must preserve solo-owner normal merge flow | Recommend minimal protection with validate, not multiple reviewer bureaucracy; review exact configuration before enabling |
| Fork policy: first-time only | All-external approval would prevent repeat-contributor trust assumptions | Native setting; adds review only for external contributions | Recommend all_external_contributors; own/Codex same-repo PRs remain automatic |

Availability/benefits are supported by [GitHub security features](https://docs.github.com/en/code-security/getting-started/github-security-features) and [push protection](https://docs.github.com/en/code-security/concepts/secret-security/push-protection); actual observed configuration above controls over defaults in documentation. See [fork approval guidance](https://docs.github.com/en/actions/how-tos/manage-workflow-runs/approve-runs-from-forks) for reviewing changes before execution. No account plan or optional billing entitlement was changed.

## Existing safeguards and recovery reused

No duplicate integration/credential/backlog/ADR/backup system is needed:

- [Integration inventory](../../integrations-side-effects.md) and `scripts/integration-registry.mjs` already cover optional Resend, Turnstile, Google Maps, private calendar, link-only PPC/social profiles and internal-only analytics. Live SMS/payment/webhooks are absent, not secretly enabled.
- `.env.example` and deployment-hosting document names, server/public classification, requirements and environment/secret-store boundaries; production values and private account identifiers stay out of Git.
- [Backup/recovery](../../backup-disaster-recovery.md) and [continuity](../../business-continuity.md) already identify hosting, GitHub, DNS/email, PPC and account-recovery dependencies. Source history does not back up provider records. Actual provider exportability, retention, account recovery, last deployed/rollback SHA and active-care offline method remain unverified/unresolved where those records say so; bootstrap did not access or change them.
- No app database/auth/client record store/uploads/durable queue exists. Future persistence requires classification, minimization, retention/ownership and recoverable migrations; do not create a shadow PPC database.
- Existing business/time/API/state/E2E suites provide a small regression foundation. Add source/rationale to relevant cases when fixing real findings; do not duplicate raw rates or silently rewrite expected business outcomes.
- Robots/sitemap are opt-in via SITE_INDEXING_ENABLED, default off. Structured data uses centralized public locality/social facts; 12G already records the outstanding locality authority gate. No SEO, media, consent or client-facing claim change was made.
- Existing secret scans, dependency review, synthetic provider tests, privacy boundaries and exact-SHA release rules are preserved. Native security feature recommendations are configuration-specific, not a reason to install a new service.

## Proposed pilot — recommendation only

| Field | Recommendation |
| --- | --- |
| PILOT_WORK_ITEM | `12G-CARE-01` — enforce the approved CarePlanner household review boundary |
| WHY | Existing P1 with precise reproduction/authority and a narrow testable correction; avoids starting optional work while application GO is blocked |
| RISK | HIGH business/safety relevance despite small diff; justified exception to LOW/MEDIUM pilot preference |
| IMPLEMENTER_MODEL / REASONING | `gpt-5.6-terra` / medium |
| REVIEWER_MODEL / REASONING | Fresh `gpt-5.6-terra` / medium; escalate on unexpected complexity |
| SENTINEL_REQUIRED | YES; deterministic tooling, `gpt-5.6-luna` / low if a model is needed |
| BUSINESS_TRUTH_REQUIRED | YES; `gpt-5.6-terra` / medium, read-only approved-source check |
| SECURITY_REVIEW_REQUIRED | NO separate security review unless the actual diff expands privacy/security boundaries |
| PARALLEL_SAFE | NO |
| EXPECTED_CHANGE_SURFACE | `app/lib/care-planner.ts`, `tests/care-planner.test.ts`, focused `e2e/public-flows.spec.ts` regression if needed, task evidence/status |
| ACCEPTANCE_SOURCE | 12G §R `12G-CARE-01`, §W item 2; CURRENT / APPROVED logic/33 §§2–3 |
| ACCEPTANCE | Approved review boundary stops ordinary guidance; just-below-boundary ordinary case stays correct; independent review conditions remain; no private rationale newly exposed; targeted and required full/hosted checks pass |

No pilot implementation, all-P1 repair, Phase 12H generation, merge or deployment is authorized by bootstrap.

## Process self-test and simplification

Conceptual simulation only, with no product edit:

1. Lauren explicitly authorizes the pilot and named settings/task creation. Orchestrator refreshes Git/CI/status and obtains approved-source evidence; records one owner/base, exact scope, tests and report location.
2. Worker ACKs before editing in an isolated verified worktree. Repeated assignment ID returns existing state; unexpected shared files or authority conflict pauses dependent work.
3. Worker implements/tests/commits, pushes a same-repository branch and opens a PR. Existing routine CI runs without a privilege expansion. FAIL goes back to its owner; PASS is checked against the worker's final SHA.
4. Fresh reviewer reads that exact diff and evidence, returns bounded findings; fixes create a new final SHA with renewed required CI. Reviewer verifies prior findings and fix-caused regressions.
5. Orchestrator checks owner/base/change surface/CI_SHA/REVIEWED_SHA/business truth, records completion, and stops. A later separately authorized merge creates new integration evidence. Application remains NO-GO until the remaining P1s and reconciled audit are complete.
6. Lost task/tool/message uses the same RUN record and preserved Git diff/branch; no guaranteed delivery, duplicate implementer, lost context, auto-deployment, or automatic optional roadmap is assumed.

Compression: one routinely read guide, one short status cache, one shared templates file, this non-routine audit. Logical specialist roles are invoked only when useful. No five prompt files, permanent idle Implementers, HANDOFFS folder, new scripts/dependencies/workflows, bot, timer, service, inventory or duplicate business table.

## Validation and completion

- Clean worktree baseline verified; canonical end comparison matched all 61 status entries and captured changed/untracked-file content hashes. Canonical HEAD remained 3b443a6; all bootstrap writes are in the separate worktree.
- Pinned local setup installed the existing lockfile because this new worktree had no dependencies; cached Chromium reused; lockfile unchanged. Initial missing-dependency doctor failure was ENVIRONMENT_FAILURE, resolved by the established setup. One existing ESLint deprecation warning was retained without upgrading.
- Doctor passed after setup. Git/recovery safety and current secret scan passed (zero secret findings).
- Initial path check correctly caught the audit file while it was not yet created; subsequent full and final path checks passed after creation.
- Full local validation passed: all safety gates and current secret scan; 172/172 Node tests, typecheck, lint with zero errors/warnings, Vinext build, build-artifact safety (187 files / 10.49 MiB), and 35/35 Playwright tests. Provider writes remained off and synthetic.
- Expected warnings: the two existing large images, five documented optional/WASM entries, Node experimental type stripping, Vinext static route-classification limitations, terminal color interaction, and branch tracking before first push. No assertions or guards were weakened.
- Independent fresh-context reviewer bootstrap_review approved the actual draft diff with no required findings; all 64 local Markdown links resolved and conceptual onboarding/lifecycle/recovery passed. Its optional exact-path clarification was applied. Final evidence-only updates receive focused verification before closure.
- Final source identity, hosted CI and review evidence are attached to the resulting PR/commit, not copied from the base run. The immutable ledger records this commit-time checkpoint; a replacement Orchestrator resolves the branch PR and its exact checks. This avoids endlessly committing each new evidence SHA into itself.
